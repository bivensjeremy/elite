const LayoutContainer = ({ children }) => {
    return (
        <section className="min-h-screen py-10  m-auto text-justify">
            {children}
        </section>
    );
}

export default LayoutContainer;